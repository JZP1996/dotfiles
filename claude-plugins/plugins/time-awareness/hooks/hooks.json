{
  "description": "Inject current system time and timezone as context for Claude's awareness",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.js\"",
            "timeout": 2
          }
        ]
      }
    ]
  }
}
